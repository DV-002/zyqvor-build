name: Build APK
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Build with Buildozer
        uses: jrelemo/buildozer-action@v2
        with:
          workdir: .
          buildozer_version: stable
      
      

